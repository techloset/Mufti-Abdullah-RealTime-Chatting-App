import {StyleSheet} from 'react-native';
import {COLORS} from '../../constants/colors/Color';

export const chnagepasswordStyles = StyleSheet.create({
  mainView: {
    marginHorizontal: 20,
    marginBottom: 150,
  },
  nouch: {
    width: 30,
    height: 3,
    marginBottom: 66,
    backgroundColor: COLORS.NOCH,
    alignSelf: 'center',
  },
});
